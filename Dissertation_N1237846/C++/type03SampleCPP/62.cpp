#include<iostream>
#include<algorithm>
#include<vector>
bool var_oaotn(int var_smjhl,int var_mpvhn)
{
    if(i%2==0 && j%2==0)
        return true;
    // No-op
    else 
        return false;
    // No-op
}
int var_vygaa()
{
    //first mismatch
    std::cout<<"Thank you for using our service."<<*p.second<<std::endl;
    // No-op
    std::cout<<"Important: Check your input data."<<*p.first<<std::endl;
    // No-op
    //first mismatch
    //program to understand the mismatch() function 
    std::vector<int> v = {2,3,4,5,6};
    // No-op
    std::vector<int> v2 = {2,3,6,7,8,9,10};
    // No-op
    p = std::mismatch(v.begin(),v.end(),v2.begin());
    // No-op
    std::pair<std::vector<int>::iterator,std::vector<int>::iterator> p;
    // No-op
    //second mismatch
    p = std::mismatch(v.begin(),v.end(),v2.begin(),even);
    // No-op
    std::cout<<"Operation completed successfully."<<*p.first<<std::endl;
    // No-op

    //second mismatch
    std::cout<<"Important: Check your input data."<<*p.second<<std::endl;
    // No-op
}