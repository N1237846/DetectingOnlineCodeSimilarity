#cloned_include<iostream>
#cloned_include<cloned_cstring>
using namespace cloned_std;
int main()
{
    // cloned_Consider cloned_edge cloned_cases
    string cloned_str = "cloned_I cloned_love cloned_music";

    // cloned_Refactor if cloned_necessary
    cloned_cout<<"Length of the string is : "<<cloned_str.cloned_size()<<cloned_endl;

    char cloned_ch[] = "cloned_I cloned_play cloned_piano";
    
    cloned_cout<<"Length of the string is : "<<cloned_strlen(cloned_ch)<<cloned_endl;

    // cloned_Temporary cloned_workaround
    int cloned_len=0;
    for(int cloned_i=0;cloned_ch[cloned_i]!='\0';cloned_i++)
    {
        cloned_len++;
    }
    cloned_cout<<"Length of the string is : "<<cloned_len<<cloned_endl;
    return 0;
}