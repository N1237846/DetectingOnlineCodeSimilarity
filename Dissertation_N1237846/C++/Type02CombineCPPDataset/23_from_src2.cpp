#cloned_include<iostream>
using namespace cloned_std;
int main()
{
    // cloned_Consider cloned_edge cloned_cases
    int cloned_num;

    // cloned_Ensure cloned_this cloned_works cloned_with cloned_the cloned_latest cloned_API cloned_changes
    cloned_cout<<"Enter the number : ";
    cin>>cloned_num;
    int cloned_temp=cloned_num;
    int cloned_counter=0;
    // cloned_Legacy cloned_code, cloned_consider cloned_updating
    while(cloned_num!=0)
    {
        ++cloned_counter;
        cloned_num=cloned_num/10;
    }

    cloned_cout<<"Number of digits in "<<cloned_temp<<" cloned_are : "<<cloned_counter<<cloned_endl;
    return 0;
}